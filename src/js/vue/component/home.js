'use strict';

const Home = {
    template: vueTemplates.home, // eslint-disable-line no-undef
    data: function() {
        return {
            msg: 'filled with data attribute'
        };
    }
};
